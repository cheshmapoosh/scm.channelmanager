spring:
  application:
    name: cache
  config:
    import: 'optional:configserver:'
    activate:
      on-profile: prod
  cloud:
    config:
      username: scm
      password: scm
      name: cache
      enabled: true
      uri: http://scm-config-server:8088/config