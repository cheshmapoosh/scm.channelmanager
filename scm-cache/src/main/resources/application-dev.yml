
spring:
  config:
    import: 'optional:configserver:'
    activate:
      on-profile: dev
  cloud:
    config:
      username: scm
      password: scm
      name: cache
      enabled: true
      uri: http://10.10.4.125:8888/config
      retry:
        max-attempts: 10
        max-interval: 3000