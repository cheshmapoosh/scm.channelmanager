server:
  port: 8888
spring:
  application:
    name: scm-config-server
  security:
    user:
      name: admin
      password: admin
  cloud:
    config:
      server:
        git:
          uri: /home/pwausr/scm/config-repo-server
#            uri: /home/scm/docker/config/config-repo-server   for DOCKER
          search-paths: '{application}'
          clone-on-start: true
          default-label: master
          basedir: config-repo
        encrypt:
          enabled: true
        prefix: /config
scm:
  config:
    applications:
      scm: ib4dev ,ib4test ,ib4prod ,scm4dev ,scm4test ,scm4prod , mb4dev, mb4test, mb4prod, pwa4dev, pwa4test, pwa4prod
      uaa: dev, test, prod
      cache: dev, test, prod
    scm-user:
      name: scm
      pass: scm
    cfg-user:
      name: cfg
      pass: cfg
    opr-user:
      name: opr
      pass: opr
encrypt:
  key: "WW@MwjtA9znCPVMi"