apiVersion: v1
kind: Service
metadata:
  name: {{ include "scm-uaa.fullname" . }}
  labels:
    {{- include "scm-uaa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scm-uaa.selectorLabels" . | nindent 4 }}
