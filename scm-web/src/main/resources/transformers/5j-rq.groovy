package transformers

def b = exchange.in.body; def f = { v, n -> v = v?.toString() ?: ''; v.length() > n ? v[0..<n] : v.padRight(n, ' ') }; def p = b['privileges'] ?: []; def t = new StringBuilder(); t << (p.contains('XFER_ADD') ? '1' : '0') << (p.contains('ACH_XFER_ADD') ? '1' : '0') << (p.contains('RTGS_XFER_ADD') ? '1' : '0') << (p.contains('IP_XFER_ADD') ? '1' : '0'); t = f(t, 10); def ch = b['PERMITSERVICEID'] ?: []; def ps = new StringBuilder(); ps << (ch.contains('ATM') ? '1' : '0') << (ch.contains('MB') ? '1' : '0') << (ch.contains('IB') ? '1' : '0'); ps = f(ps, 10); return f(b.accountNo, 18) + f(b.nationalCode, 10) + f(b.cardNo, 20) + f(b.customerCount, 1) + f(b.customerNoMain, 12) + f(b.customerNo2, 12) + f(b.customerNo3, 12) + f(b.customerNo4, 12) + f(b.customerNo5, 12) + f(b.customerNo6, 12) + f(b.customerNo7, 12) + f(b.customerNo8, 12) + f(b.customerNo9, 12) + f(b.insDel, 1) + t + f(b.maxInternalAmount, 18) + f(b.maxPayaAmount, 18) + f(b.maxSatnaAmount, 18) + f(b.maxIpAmount, 18) + f(b.expireDate, 8) + ps