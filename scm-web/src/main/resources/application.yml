spring:
  profiles:
    active: default
  application:
    name: ${SCM_APPLICATION_NAME}
scm:
  cache:
    client:
      caches:
        scm-web-api-doc-catalog:
          type: local
          ttl: 60s
          maximum-size: 128
  application:
    version: 9.0.0
    build:  0521T1030
